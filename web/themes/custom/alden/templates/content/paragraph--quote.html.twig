{#
  Glue: quote paragraph -> alden:pull-quote SDC. Props are built in
  AldenHooks::preprocessParagraph() (see src/Hook/AldenHooks.php).
#}
{{ include('alden:pull-quote', alden, with_context = false) }}
