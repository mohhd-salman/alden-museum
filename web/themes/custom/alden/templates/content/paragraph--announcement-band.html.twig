{#
  Glue: announcement_band paragraph -> alden:announce SDC. Props are built
  in AldenHooks::preprocessParagraph() (see src/Hook/AldenHooks.php).
#}
{{ include('alden:announce', alden, with_context = false) }}
