{#
  Glue: card_grid paragraph -> alden:cards SDC (the homepage's
  first-card-spans-two exhibitions grid). Props are built in
  AldenHooks::preprocessParagraph() (see src/Hook/AldenHooks.php).
#}
{{ include('alden:cards', alden, with_context = false) }}
