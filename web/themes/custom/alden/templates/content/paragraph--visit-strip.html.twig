{#
  Glue: visit_strip paragraph -> alden:visit SDC. Props are built in
  AldenHooks::preprocessParagraph() (see src/Hook/AldenHooks.php).
#}
{{ include('alden:visit', alden, with_context = false) }}
